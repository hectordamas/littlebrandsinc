$(document).ready(function() {
    // Initialize Select2 on all select elements with the class 'select2'
    $('.flag-select-2').select2({
        width: '100%' // Ensure the Select2 dropdown takes the full width of its container
    });
});